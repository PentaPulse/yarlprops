# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci
      - run: echo "successfully cl outer"
      - run: npm ci --prefix main-app
      - run: echo "successfully cl main"
      - run: npm ci --prefix admin-app
      - run: echo "successfully cl admin"
      - run: npm run build --prefix admin-app
        env:
          CI: false
      - run: echo "successfully build admin build"
      - run: npm run build --prefix main-app
        env:
          CI: false
      - run: echo "successfully build main build"
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_YARLPROPS }}
          channelId: live
          projectId: yarlprops
